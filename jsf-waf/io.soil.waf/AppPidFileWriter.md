# `AppPidFileWriter`

`package io.soil.waf`

extends `ApplicationPidFileWriter`

应用 PID 文件写入器，将应用进程 ID 写入 ./app.pid 文件。

---

## Methods

### `AppPidFileWriter()`

构造 AppPidFileWriter，PID 文件路径为 ./app.pid