# DateTimeConst

> 日期时间常量类

- **包**: io.soil.jsf.common.date
- **修饰**: `final`（不可继承）

## 常量

| 常量 | 类型 | 值 |
|------|------|-----|
| `SYSTEM_ZONE_OFFSET` | `ZoneOffset` | 系统默认时区偏移量 |

> 构造器为 private，不可实例化。